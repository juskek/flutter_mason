import 'package:wordsmith_app/utils/index.dart';

class PressableContainer extends StatelessWidget {
  const PressableContainer({required this.child, Key? key}) : super(key: key);
  final Widget child;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
